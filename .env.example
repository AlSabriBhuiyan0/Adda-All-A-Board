# PostgreSQL (required)
# With Docker: docker-compose up -d then use:
DATABASE_URL=postgresql://adda:adda_secret@localhost:5432/adda_games

# JWT
SESSION_SECRET=your-secret-key

# Redis (optional - for leaderboard cache, logout blacklist, reconnection)
# Option 1: Full URL (use for Redis Labs or any remote Redis)
# REDIS_URL=redis://default:YOUR_PASSWORD@redis-xxxxx.cloud.redislabs.com:18297
# Option 2: Local Docker
REDIS_URL=redis://127.0.0.1:6379

# Option 3: Build URL from parts (uncomment and set if not using REDIS_URL)
# REDIS_USERNAME=default
# REDIS_PASSWORD=your_redis_password
# REDIS_HOST=redis-18297.c44.us-east-1-2.ec2.cloud.redislabs.com
# REDIS_PORT=18297

# Server
PORT=5000
